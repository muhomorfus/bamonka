\begin{definitions}
	\definition{Статический сервер}{программа, позволяющая клиентам получать доступ к файлам на сервере~\cite{intro}.}
	
	\definition{Сокет}{абстакция конечной точки соединения~\cite{ryaznu}.}
	
	\definition{Протокол}{набор соглашений логического уровня~\cite{tikh}.}
\end{definitions}